# Write a Python function to check whether a number falls in a given range.
def check_number_falls():
    given_range = range(1,21)
    num = int(input("Enter the number: "))
    print(num in given_range)


check_number_falls()
